#include <stdio.h>

int main()
{
    char name[] = "Harsh";
    char rst[40] = "Do it Now.";
    char uinp[20];

    printf("Name is %s\n", name);

    scanf("%s", uinp);
    printf("User input is %s", uinp);

    

    return 0;
}
